package com.scarabcoder.gameapi.npc;

public class NPCManager {
	
	/*private static HashMap<UUID, NPC> npcs = new HashMap<UUID, NPC>();
	
	
	
	public static boolean isNPC(Entity entity){
		return npcs.get(entity.getUniqueId()) != null;
	}
	
	public static void storeNPC(NPC npc){
		npcs.put(npc.getEntity().getUniqueId(), npc);
	}
	
	public static NPC getNPC(Entity entity){
		return npcs.get(entity.getUniqueId());
	}*/
	
}
